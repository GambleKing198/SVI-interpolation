**SVI** is mostly used to construct the IV surface, especially when extreme **tail risks** exist. 
Then the code is following by a simplified example of its **calibration** that makes the **IV fits the market data**
